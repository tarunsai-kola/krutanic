# 🎓 VISUAL LEARNING GUIDE - Backend Architecture

## COMPLETE SYSTEM ARCHITECTURE

```
┌─────────────────────────────────────────────────────────────────┐
│                     FRONTEND (React)                            │
│                   http://localhost:5173                         │
│                                                                 │
│  - User Interface                                              │
│  - Makes HTTP requests                                         │
│  - Stores JWT token in localStorage                            │
│  - Displays responses                                          │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     │ HTTP Requests (JSON)
                     │ with Authorization header
                     │
                     ↓
┌─────────────────────────────────────────────────────────────────┐
│                   EXPRESS SERVER                                │
│                 (Node.js Backend)                               │
│            http://localhost:5000                                │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ server.js - Entry Point                                 │  │
│  │ - Loads all routes                                      │  │
│  │ - Registers middleware                                  │  │
│  │ - Connects to database                                  │  │
│  │ - Starts listening on port 5000                         │  │
│  └──────────────────────────────────────────────────────────┘  │
│                          │                                      │
│  ┌──────────────────────↓──────────────────────────────────┐  │
│  │ MIDDLEWARE LAYER                                        │  │
│  │ ─────────────────────────────────────────────────────  │  │
│  │ 1. CORS Middleware                                     │  │
│  │    - Check if request origin is allowed                │  │
│  │    - Allow credentials                                 │  │
│  │                                                        │  │
│  │ 2. Body Parser Middleware                              │  │
│  │    - Convert JSON to JavaScript objects               │  │
│  │                                                        │  │
│  │ 3. Auth Middleware (UserAuth.js)                       │  │
│  │    - Extract JWT token from Authorization header       │  │
│  │    - Verify token signature                            │  │
│  │    - Attach user to req.user                           │  │
│  │    - Block request if token invalid                    │  │
│  │                                                        │  │
│  │ 4. Cloudinary Middleware                               │  │
│  │    - Configure file upload service                     │  │
│  │    - Parse multipart form data                         │  │
│  └──────────────────────────────────────────────────────────┘  │
│                          │                                      │
│  ┌──────────────────────↓──────────────────────────────────┐  │
│  │ ROUTE LAYER (routes/*.js)                               │  │
│  │ ─────────────────────────────────────────────────────   │  │
│  │                                                        │  │
│  │ ┌─────────────────────────────────────────────────┐   │  │
│  │ │ User Routes (/users)                            │   │  │
│  │ │ POST   /users         → Create user             │   │  │
│  │ │ GET    /users         → Get all users           │   │  │
│  │ │ PUT    /users/:id     → Update user             │   │  │
│  │ │ POST   /checkuserauth → Login                   │   │  │
│  │ │ POST   /send-otp      → Send OTP                │   │  │
│  │ │ POST   /verify-otp    → Verify OTP              │   │  │
│  │ └─────────────────────────────────────────────────┘   │  │
│  │                                                        │  │
│  │ ┌─────────────────────────────────────────────────┐   │  │
│  │ │ Course Routes (/course)                         │   │  │
│  │ │ POST   /createcourse  → Create course           │   │  │
│  │ │ GET    /getcourses    → Get courses             │   │  │
│  │ │ PUT    /editcourse    → Update course           │   │  │
│  │ │ DELETE /deletecourse  → Delete course           │   │  │
│  │ └─────────────────────────────────────────────────┘   │  │
│  │                                                        │  │
│  │ ┌─────────────────────────────────────────────────┐   │  │
│  │ │ More Routes...                                  │   │  │
│  │ │ - Job routes                                    │   │  │
│  │ │ - Event routes                                  │   │  │
│  │ │ - Admin routes                                  │   │  │
│  │ │ - Mentorship routes                             │   │  │
│  │ │ - Certificate routes                            │   │  │
│  │ │ - etc.                                          │   │  │
│  │ └─────────────────────────────────────────────────┘   │  │
│  │                                                        │  │
│  │ Each route:                                            │  │
│  │ 1. Receives request                                   │  │
│  │ 2. Validates input                                    │  │
│  │ 3. Calls model for database operation                │  │
│  │ 4. Processes response                                │  │
│  │ 5. Sends JSON response back                           │  │
│  │                                                        │  │
│  └──────────────────────────────────────────────────────────┘  │
│                          │                                      │
│  ┌──────────────────────↓──────────────────────────────────┐  │
│  │ DATABASE LAYER (models/*.js)                            │  │
│  │ ─────────────────────────────────────────────────────   │  │
│  │                                                        │  │
│  │ User.js              MongoDB Collection: users        │  │
│  │ ├─ fullname: String                                   │  │
│  │ ├─ email: String (unique)                             │  │
│  │ ├─ phone: String                                      │  │
│  │ ├─ password: String                                   │  │
│  │ ├─ otp: String                                        │  │
│  │ └─ ... 10+ more fields                                │  │
│  │                                                        │  │
│  │ CreateCourse.js      MongoDB Collection: createcourses│  │
│  │ ├─ title: String                                      │  │
│  │ ├─ description: String                                │  │
│  │ └─ sessions: Map of session objects                   │  │
│  │                                                        │  │
│  │ More Models...                                         │  │
│  │ ├─ CreateJob                                          │  │
│  │ ├─ JobApplication                                     │  │
│  │ ├─ Certificate                                        │  │
│  │ ├─ EventApplication                                   │  │
│  │ ├─ Mentorship                                         │  │
│  │ ├─ CreateBDA                                          │  │
│  │ ├─ CreateOperation                                    │  │
│  │ ├─ CreateMarketing                                    │  │
│  │ └─ ... 15+ more models                                │  │
│  │                                                        │  │
│  └──────────────────────────────────────────────────────────┘  │
│                          │                                      │
└────────────────────┬─────────────────────────────────────────────┘
                     │
                     │ Mongoose ODM
                     │ Query operations
                     │
                     ↓
┌─────────────────────────────────────────────────────────────────┐
│                    MONGODB DATABASE                             │
│                (Cloud: MongoDB Atlas)                           │
│                                                                 │
│  Database: krutanic                                            │
│  Collections:                                                   │
│  ├─ users          (23,450 documents)                          │
│  ├─ createcourses  (45 documents)                              │
│  ├─ createjobs     (120 documents)                             │
│  ├─ jobapplications (5,230 documents)                          │
│  ├─ certificates   (18,900 documents)                          │
│  ├─ eventapplications (8,760 documents)                        │
│  ├─ addtransactionids (42,100 documents)                       │
│  └─ ... more collections                                       │
│                                                                 │
│  Each document is a JSON-like object with unique _id           │
└─────────────────────────────────────────────────────────────────┘

                     ↑
                     │ HTTP Response (JSON)
                     │ Status codes (200, 201, 400, 401, 404, 500)
                     │
                     ↓
             (Back to Frontend)
```

---

## REQUEST/RESPONSE CYCLE

### Example: User Login

```
STEP 1: USER INITIATES REQUEST
┌────────────────────────────────┐
│ Frontend (React)               │
│ User clicks "Login"            │
│ POST /checkuserauth            │
│ Body: {                        │
│   "email": "john@...com",      │
│   "password": "Krutanic@123"   │
│ }                              │
└────────────────┬───────────────┘
                 │
                 ↓
STEP 2: MIDDLEWARE PROCESSES
┌────────────────────────────────┐
│ Express Server                 │
│ 1. CORS check ✓               │
│ 2. Parse JSON ✓               │
│ 3. No token needed for login   │
└────────────────┬───────────────┘
                 │
                 ↓
STEP 3: ROUTE HANDLER EXECUTES
┌────────────────────────────────┐
│ routes/User.js                 │
│ POST /checkuserauth            │
│                                │
│ 1. Get email & password        │
│ 2. Find user in database       │
│ 3. Check if user exists        │
│ 4. Verify password matches     │
│ 5. Check account is active     │
│ 6. Create JWT token            │
└────────────────┬───────────────┘
                 │
                 ↓
STEP 4: DATABASE QUERY
┌────────────────────────────────┐
│ Model: User                    │
│ Query: User.findOne({ email }) │
│                                │
│ MongoDB returns:               │
│ {                              │
│   "_id": "507f1...",          │
│   "fullname": "John Doe",      │
│   "email": "john@...com",      │
│   "password": "Krutanic@123",  │
│   "status": "active",          │
│   ... more fields              │
│ }                              │
└────────────────┬───────────────┘
                 │
                 ↓
STEP 5: TOKEN GENERATION
┌────────────────────────────────┐
│ jwt.sign({                     │
│   id: user._id,                │
│   email: user.email            │
│ }, JWT_SECRET)                 │
│                                │
│ Generates token:               │
│ "eyJhbGciOiJIUzI1NiIsInR5c..."│
└────────────────┬───────────────┘
                 │
                 ↓
STEP 6: SEND RESPONSE
┌────────────────────────────────┐
│ res.status(200).json({         │
│   token: "eyJ...",             │
│   _id: "507f1...",             │
│   email: "john@...com"         │
│ })                             │
│                                │
│ HTTP 200 OK                    │
└────────────────┬───────────────┘
                 │
                 ↓
STEP 7: FRONTEND RECEIVES
┌────────────────────────────────┐
│ Frontend (React)               │
│ localStorage.setItem(          │
│   'token',                     │
│   'eyJ...'                     │
│ )                              │
│ Redirect to dashboard          │
│ User logged in! ✓              │
└────────────────────────────────┘
```

---

## AUTHENTICATION FLOW VISUALIZATION

```
BEFORE LOGIN:
┌─────────────────┐
│ No token        │
│ Cannot access   │
│ protected       │
│ endpoints       │
└─────────────────┘
         │
         ↓
LOGIN (POST /checkuserauth)
    User email + password
         │
         ↓
   Verify credentials
         │
         ├─→ Invalid → Return 401 Unauthorized
         │
         ├─→ Account inactive → Return 403 Forbidden
         │
         └─→ Valid → Generate JWT Token

AFTER LOGIN:
┌─────────────────────────────┐
│ Client Storage              │
│ localStorage[token] =       │
│ "eyJhbGciOiJIUzI1..."       │
│                             │
│ Every request includes:     │
│ Authorization: <token>      │
└──────────────┬──────────────┘
               │
               ↓
┌─────────────────────────────┐
│ Protected Route Access      │
│                             │
│ GET /dashboard              │
│ GET /profile                │
│ POST /create-course         │
│ PUT /update-user            │
│ DELETE /remove-data         │
│                             │
│ All work with valid token   │
│ All fail without token      │
│ All fail with invalid token │
└─────────────────────────────┘
```

---

## REQUEST TYPES & RESPONSES

### TYPE 1: SIMPLE GET (Fetch Data)

```
REQUEST:
┌──────────────────────────────┐
│ GET /getcourses              │
│                              │
│ Headers: {}                  │
│ Body: (empty)                │
└──────────────┬───────────────┘
               │
               ↓
SERVER:
1. Find all courses in database
2. Sort by creation date

RESPONSE (200):
┌──────────────────────────────┐
│ [                            │
│   {                          │
│     _id: "507f1...",         │
│     title: "Node.js",        │
│     description: "..."       │
│   },                         │
│   {                          │
│     _id: "507f2...",         │
│     title: "React",          │
│     description: "..."       │
│   }                          │
│ ]                            │
└──────────────────────────────┘
```

### TYPE 2: CREATE (POST with Data)

```
REQUEST:
┌──────────────────────────────┐
│ POST /createcourse           │
│                              │
│ Body:                        │
│ {                            │
│   "title": "Python",         │
│   "description": "Basics..." │
│ }                            │
└──────────────┬───────────────┘
               │
               ↓
SERVER:
1. Validate input
2. Create new Course object
3. Save to database
4. Get MongoDB _id

RESPONSE (201 CREATED):
┌──────────────────────────────┐
│ {                            │
│   _id: "507f3...",           │
│   title: "Python",           │
│   description: "Basics...",  │
│   session: {},               │
│   __v: 0                     │
│ }                            │
└──────────────────────────────┘
```

### TYPE 3: UPDATE (PUT with ID)

```
REQUEST:
┌──────────────────────────────┐
│ PUT /editcourse/507f1        │
│                              │
│ Body:                        │
│ {                            │
│   "title": "Advanced Node"   │
│ }                            │
└──────────────┬───────────────┘
               │
               ↓
SERVER:
1. Find course by ID
2. Update field(s)
3. Save to database
4. Return updated

RESPONSE (200):
┌──────────────────────────────┐
│ {                            │
│   _id: "507f1...",           │
│   title: "Advanced Node",    │
│   description: "...",        │
│   session: {}                │
│ }                            │
└──────────────────────────────┘
```

### TYPE 4: DELETE (DELETE with ID)

```
REQUEST:
┌──────────────────────────────┐
│ DELETE /deletecourse/507f1   │
│                              │
│ Body: (empty)                │
└──────────────┬───────────────┘
               │
               ↓
SERVER:
1. Find course by ID
2. Delete from database
3. Return deleted object

RESPONSE (200):
┌──────────────────────────────┐
│ {                            │
│   _id: "507f1...",           │
│   title: "Node.js",          │
│   description: "...",        │
│   session: {}                │
│ }                            │
│                              │
│ (Resource is now deleted)    │
└──────────────────────────────┘
```

### TYPE 5: PROTECTED ROUTE (Requires Token)

```
REQUEST:
┌──────────────────────────────┐
│ GET /dashboard               │
│                              │
│ Headers:                     │
│ Authorization: eyJ...        │
│ (JWT Token from login)       │
└──────────────┬───────────────┘
               │
               ↓
MIDDLEWARE (UserAuth.js):
1. Extract token from header
2. Verify token signature
3. Check if expired
4. Attach user to req.user

RESPONSE (200):
├─→ Token valid → Return dashboard data
└─→ Token invalid/missing → Return 401 Unauthorized
```

---

## ERROR HANDLING

```
CLIENT SENDS REQUEST
        │
        ↓
SERVER RECEIVES
        │
   ┌────┴────┬─────────┬──────────┐
   │          │         │          │
   ↓          ↓         ↓          ↓
Missing    Invalid   Logic      Server
Data       Format    Error      Error
   │          │         │          │
   ↓          ↓         ↓          ↓
400         400       404/403    500
Bad       Bad        Not        Server
Request   Request    Found      Error

┌─────────────────────────────────┐
│ Try-Catch Block in Every Route  │
│                                 │
│ try {                           │
│   // Database operation         │
│   // Logic here                 │
│   // Send response              │
│ } catch (error) {               │
│   res.status(500).json({        │
│     message: "Error"            │
│   })                            │
│ }                               │
└─────────────────────────────────┘
```

---

## DATABASE OPERATIONS FLOW

```
┌──────────────────────────────────┐
│ Route Handler in Express         │
└───────────────┬──────────────────┘
                │
    ┌───────────┼───────────┬──────────────┬──────────┐
    │           │           │              │          │
    ↓           ↓           ↓              ↓          ↓
  CREATE      READ        UPDATE        DELETE    FIND
   │           │           │              │          │
   ↓           ↓           ↓              ↓          ↓

new Model()  Model.find() Model.findByIdAndUpdate()  Model.findByIdAndDelete()
.save()      Model.findOne() 
             Model.findById()

    │           │           │              │          │
    └───────────┼───────────┴──────────────┴──────────┘
                │
                ↓
        ┌───────────────────┐
        │ MONGOOSE ODM      │
        │ (Object Data      │
        │  Modeling)        │
        │                   │
        │ - Validates data  │
        │ - Applies schema  │
        │ - Handles types   │
        └───────┬───────────┘
                │
                ↓
        ┌───────────────────┐
        │ MONGODB DRIVER    │
        │                   │
        │ Creates queries   │
        │ Sends to MongoDB  │
        │ Receives results  │
        └───────┬───────────┘
                │
                ↓
        ┌───────────────────┐
        │ MONGODB DATABASE  │
        │                   │
        │ Executes query    │
        │ Modifies data     │
        │ Returns result    │
        └───────┬───────────┘
                │
                ↓
        ┌───────────────────┐
        │ RESULT RETURNED   │
        │                   │
        │ - Document(s)     │
        │ - Error if fails  │
        │ - null if none    │
        └───────┬───────────┘
                │
                ↓
        ┌───────────────────┐
        │ ROUTE HANDLER     │
        │ Processes result  │
        │ Sends response    │
        └───────────────────┘
```

---

## FILE UPLOAD FLOW

```
┌─────────────────────────────────┐
│ Client (React)                  │
│ - Select file                   │
│ - FormData with file            │
│ - POST to /upload               │
└──────────────┬──────────────────┘
               │
               ↓
┌─────────────────────────────────┐
│ Express Server                  │
│ - Receive multipart form data   │
│ - Parse with formidable/busboy  │
│ - Extract file from temp folder │
└──────────────┬──────────────────┘
               │
               ↓
┌─────────────────────────────────┐
│ Cloudinary Middleware           │
│ - Connect to Cloudinary service │
│ - Upload file to cloud          │
│ - Get secure URL back           │
│ - Delete local temp file        │
└──────────────┬──────────────────┘
               │
               ↓
┌─────────────────────────────────┐
│ Cloudinary (Cloud Service)      │
│ - Store file on CDN             │
│ - Generate public URL           │
│ - Return to backend             │
└──────────────┬──────────────────┘
               │
               ↓
┌─────────────────────────────────┐
│ Express Server                  │
│ - Save URL to database          │
│ - Return response with URL      │
└──────────────┬──────────────────┘
               │
               ↓
┌─────────────────────────────────┐
│ Client (React)                  │
│ - Display uploaded file         │
│ - File is now publicly available│
└─────────────────────────────────┘
```

---

## EMAIL SENDING FLOW

```
┌────────────────────────────────┐
│ Route Handler                  │
│ (e.g., POST /send-otp)         │
└────────────┬───────────────────┘
             │
             ↓
┌────────────────────────────────┐
│ Generate Email Content         │
│ - Create HTML template         │
│ - Insert dynamic data          │
│ - Format nicely                │
└────────────┬───────────────────┘
             │
             ↓
┌────────────────────────────────┐
│ Call sendEmail Controller      │
│ - emailController.js           │
│ - Pass recipient, subject, msg │
└────────────┬───────────────────┘
             │
             ↓
┌────────────────────────────────┐
│ Nodemailer                     │
│ - Connect to Gmail SMTP        │
│ - Use credentials from .env    │
│ - Send email                   │
└────────────┬───────────────────┘
             │
             ↓
┌────────────────────────────────┐
│ Gmail Server                   │
│ - Receive email                │
│ - Send to recipient            │
│ - Arrive in inbox              │
└────────────┬───────────────────┘
             │
             ↓
┌────────────────────────────────┐
│ User's Email                   │
│ - Opens email                  │
│ - Sees content                 │
│ - May click links/buttons      │
└────────────────────────────────┘
```

---

## COMPLETE REQUEST JOURNEY

```
┌────────────────────────────────────────────────────────────┐
│ 1. USER CLICKS BUTTON IN FRONTEND (React)                  │
│    "Create Course"                                         │
└────────┬───────────────────────────────────────────────────┘
         │
         ↓
┌────────────────────────────────────────────────────────────┐
│ 2. JAVASCRIPT SENDS HTTP REQUEST                           │
│    axios.post('/createcourse', {                           │
│      title: "...",                                         │
│      description: "..."                                    │
│    })                                                      │
└────────┬───────────────────────────────────────────────────┘
         │
         ↓ Network → Internet → Server
         │
┌────────────────────────────────────────────────────────────┐
│ 3. EXPRESS SERVER RECEIVES REQUEST                         │
│    POST http://localhost:5000/createcourse                 │
│    Body: { title: "...", description: "..." }              │
└────────┬───────────────────────────────────────────────────┘
         │
         ↓
┌────────────────────────────────────────────────────────────┐
│ 4. MIDDLEWARE PROCESSES                                    │
│    ✓ CORS check passes                                     │
│    ✓ JSON parsing works                                    │
│    ✓ No auth needed for POST /createcourse                │
└────────┬───────────────────────────────────────────────────┘
         │
         ↓
┌────────────────────────────────────────────────────────────┐
│ 5. ROUTE HANDLER EXECUTES (routes/CreateCourse.js)         │
│    router.post("/createcourse", async (req, res) => {      │
│      const { title, description } = req.body;             │
│      try {                                                 │
│        const course = new CreateCourse({...});             │
│        await course.save();                                │
│        res.status(201).json(course);                       │
│      } catch (error) { ... }                               │
│    })                                                      │
└────────┬───────────────────────────────────────────────────┘
         │
         ↓
┌────────────────────────────────────────────────────────────┐
│ 6. DATABASE OPERATION (Model: CreateCourse)                │
│    - Validate data against schema                          │
│    - Create document with unique _id                       │
│    - Insert into 'createcourses' collection                │
│    - Return saved document                                 │
└────────┬───────────────────────────────────────────────────┘
         │
         ↓
┌────────────────────────────────────────────────────────────┐
│ 7. RESPONSE GENERATED                                      │
│    Status: 201 Created                                     │
│    Body: {                                                 │
│      "_id": "507f1f77bcf86cd799439011",                   │
│      "title": "...",                                       │
│      "description": "...",                                 │
│      "session": {},                                        │
│      "__v": 0                                              │
│    }                                                       │
└────────┬───────────────────────────────────────────────────┘
         │
         ↓ Network → Internet → Browser
         │
┌────────────────────────────────────────────────────────────┐
│ 8. FRONTEND RECEIVES RESPONSE                              │
│    axios.post() .then(response => {                        │
│      console.log("Course created!", response.data);        │
│      // Update UI with new course                          │
│    })                                                      │
└────────────────────────────────────────────────────────────┘
```

---

*This visual guide helps you understand how requests flow through the system!*

*Refer back to these diagrams as you learn each component.*
